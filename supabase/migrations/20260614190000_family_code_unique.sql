-- Этап 1: вход в семью только по коду (без названия).
-- Хеш кода стал детерминированным (HMAC), код уникален среди всех семей →
-- вешаем уникальный индекс, чтобы БД гарантировала отсутствие дублей кода.

alter table public.families
  add constraint families_code_hash_key unique (code_hash);
