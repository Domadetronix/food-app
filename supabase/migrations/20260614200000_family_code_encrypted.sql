-- Этап 1: возможность посмотреть код семьи в настройках.
-- Храним код обратимо (AES-256-GCM, ключ выводится из CODE_PEPPER на сервере),
-- чтобы показывать его участникам. code_hash (HMAC) остаётся для поиска/уникальности.
-- Существующие семьи получат NULL — для них код можно будет только перегенерировать.

alter table public.families
  add column code_encrypted text;
